/**
 * Application-wide constants.
 *
 * Centralises magic strings and configuration values so they can be
 * changed in one place and easily located by searching for the module name.
 */

/** Default debounce delay for search inputs (milliseconds). */
export const SEARCH_DEBOUNCE_MS = 300;

/** Default workspace id when no workspace is configured. */
export const DEFAULT_WORKSPACE_ID = "workspace-local";
